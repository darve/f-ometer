{
  "name": "fuckometer",
  "version": "0.1.0",
  "private": true,
  "description": "Swear-word trend visualization dashboard",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "db:migrate": "pnpm --filter @fuckometer/api db:migrate",
    "ingest:gdelt": "pnpm --filter @fuckometer/api ingest:gdelt",
    "ingest:mastodon": "pnpm --filter @fuckometer/api ingest:mastodon"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "turbo": "^2.3.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.59.2"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  }
}
